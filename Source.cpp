#include <iostream>
#include <vector>
#include "Tester.h"
#include "BinaryReader.h"
#include "Logger.h"
#include "Defines.h"

using namespace std;

int main()
{
	/*vector <point_t> pointsVector;
	int i;
	point_t punkt;
	for (i = 0; i < 100; i++)
	{
		punkt.x = i;
		punkt.y = i + 1;
		punkt.z = i + 2;
		pointsVector.push_back(punkt);

	}
	
	CLogger loger("dane.bin", true); 
	loger.Log(punkt);
	CBinaryReader obiekt("dane.bin");
	punkt.x = 6;
	obiekt.ReadIndex(1, punkt);
	
	for (i = 0; i < 100; i++)
	{
		cout << pointsVector[i].x << ";" << pointsVector[i].y << ";" << pointsVector[i].z << ";" << endl;
	}

	cout << punkt.x << ";" << punkt.y << ";" << punkt.z << ";" << endl; */
	system("pause");
		return 0;
}